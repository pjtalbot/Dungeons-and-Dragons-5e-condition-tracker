<!-- NOT IMPLEMENTED YET -->

<div class="container-md border">
    <form action="/character/<%=character.id %>/add/immunity" method="POST">
        <label for="immunities">Add Immunity</label>
    <div class="select">
        <select name="immunities-selector" id="immunities-selector">
            <% for (let damageType of allDamageTypes.results) { %>
                <option value="<%= damageType.index %>"><%= damageType.index %></option>       
              <% } %>
        </select>
    </div>
    <button>Add Immunity
    </Button>
    
    </form>

</div>